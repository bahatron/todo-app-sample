on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main
    steps:
        - run: echo "hi there"
        - run: docker version
        - run: docker-compose version
