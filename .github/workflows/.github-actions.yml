on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main

jobs:
    build-and-test:
        runs-on: ubuntu-latest
        steps:
            - run: echo "hi there"
            - run: docker version
            - run: docker-compose version
